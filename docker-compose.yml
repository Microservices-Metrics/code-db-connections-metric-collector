services:
  api:
    build:
      context: ./src/CodeDbConnectionsMetricCollector.Api
      dockerfile: Dockerfile
    ports:
      - "8084:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
